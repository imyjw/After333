# After333 서버 동시 부하 검증 — 2026-09-11

## 결론

최대 동시 40명(20경기), 총 154개 합성 계정·77경기의 짧은 반복 부하를 통과했다. 이 범위에서는 서버 재구축이나 운영 코드 수정이 필요한 오류가 재현되지 않았다. 운영 계정·DB·서버 설정은 변경하지 않았다.

## 측정 결과

최종 실행 기준. 지연은 동일 PC의 loopback 왕복이며 인터넷 지연을 포함하지 않는다.

| 동시 접속 | 동시 경기 | 매칭 완료 p95 | 공격·턴 종료 p95 | 재접속 p95 |
| --- | --- | --- | --- | --- |
| 4명 | 2경기 | 461ms | 26ms | 32ms |
| 10명 | 5경기 | 293ms | 22ms | 21ms |
| 20명 | 10경기 | 534ms | 20ms | 83ms |
| 40명 1회차 | 20경기 | 1,151ms | 24ms | 94ms |
| 40명 2회차 | 20경기 | 542ms | 19ms | 63ms |
| 40명 3회차 | 20경기 | 519ms | 14ms | 59ms |

- 실제 HTTP 가입·덱 생성, WebSocket 매칭, 경기마다 12턴의 마스터 공격·턴 종료, 연결 단절·복구 1회, 항복에 의한 서버 결과 저장을 수행했다.
- 공격·턴 종료 지연 표본 1,848개, 재접속 77회.
- 경기 결과 77개와 참가자 결과 154개를 확인했다. 각 계정의 기대 승패와 실제 런 성적이 일치했다.
- 모든 회차에서 연결을 닫은 후 세션 목록은 비었다. 마지막 연결 종료 후 80초 관찰 뒤 결과 저장 대기 JSON은 0개였다.
- 서버 private memory는 표본상 최고 약 311MiB, 마지막 약 185MiB였다. 강제 GC는 사용하지 않았다. 반복 구간에서 계속 증가하는 양상은 없었지만 장기 누수 부재를 보장하지 않는다.
- 최종 실행 관찰 시간은 약 118초다. PostgreSQL과 검사 클라이언트 메모리는 위 서버 수치에서 제외했다.

## 해석과 남은 범위

40명이 운영상 최대 수용 인원이라는 뜻은 아니다. 같은 PC에서 부하 생성기·DB·서버를 실행했고 인터넷/TLS 프록시/실기기 렌더링은 제외했다. 합성 계정이 모두 같은 IP에서 접속하므로 격리 서버에서만 HTTP IP 제한을 높였다. 실제 운영의 접속 허용 정책은 별개다.

이번 전투 부하는 마스터 공격과 턴 전환 중심이다. 대규모 필드의 연쇄 사망 효과·지속 마법·AI 탐색을 포함한 최악 조건, 장시간 운영, 실제 인터넷 지연은 추가 측정 대상이다. 이번 결과만으로 전체 카드 조합의 성능이나 운영 용량을 보장하지 않는다.

## 재현

`Server/Project333.LoadChecks/RunIsolatedLoadChecks.ps1`에 검증할 서버 배포 폴더를 ServerOutput으로 전달한다. 도구는 실제 계정 정보나 외부 서버 주소를 받지 않는다. 고정 격리 포트 15439/17339가 사용 중이면 기존 프로세스를 건드리지 않고 중단한다. 각 실행은 새 임시 DB를 만들고 자신이 띄운 프로세스만 종료한다.

최종 자료: TempBuild/LoadChecks_20260911의 metrics.json, load-summary.json, load-memory.json, passed-checks.log.
검증 서버 SHA256: CB9317CA982CE4C7CA2F99E64FFDEA39C9F2CBA9B80FDA56EDFC2BD78C234893.
초기 검사 도구의 컴파일/응답 필터 문제는 최종 실행 전에 고쳤으며 서버 장애로 집계하지 않았다.